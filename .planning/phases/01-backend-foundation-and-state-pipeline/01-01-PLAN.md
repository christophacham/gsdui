---
phase: 01-backend-foundation-and-state-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - build.rs
  - migrations/001_initial_schema.sql
  - src/main.rs
  - src/config.rs
  - src/state.rs
  - src/db/mod.rs
  - src/db/schema.rs
  - src/db/models.rs
  - src/api/mod.rs
  - src/api/projects.rs
  - src/api/health.rs
  - static/index.html
autonomous: true
requirements:
  - STATE-11
  - STATE-12
  - INFRA-01

must_haves:
  truths:
    - "Daemon binary compiles and starts listening on a configurable port"
    - "SQLite database is created with WAL mode and all tables from the schema migration"
    - "REST API at /api/v1/projects supports CRUD for project registration"
    - "REST API at /api/v1/health returns daemon diagnostics"
    - "Static file serving returns placeholder page at root URL"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "axum.*sqlx.*notify.*serde_yml"
    - path: "migrations/001_initial_schema.sql"
      provides: "Full database schema for all Phase 1 tables"
      contains: "CREATE TABLE projects"
    - path: "src/main.rs"
      provides: "Entry point with server startup and graceful shutdown"
      min_lines: 40
    - path: "src/state.rs"
      provides: "AppState struct shared across all handlers"
      exports: ["AppState"]
    - path: "src/db/mod.rs"
      provides: "Database pool initialization with WAL mode"
      min_lines: 15
    - path: "src/db/schema.rs"
      provides: "Query functions for all database operations"
      min_lines: 50
    - path: "src/db/models.rs"
      provides: "Database row types used by parsers and API"
      min_lines: 80
    - path: "src/api/projects.rs"
      provides: "Project CRUD endpoints"
      exports: ["router"]
  key_links:
    - from: "src/main.rs"
      to: "src/state.rs"
      via: "Arc<AppState> shared with router"
      pattern: "Arc::new.*AppState"
    - from: "src/main.rs"
      to: "src/db/mod.rs"
      via: "Database pool initialization on startup"
      pattern: "SqlitePool"
    - from: "src/api/projects.rs"
      to: "src/db/schema.rs"
      via: "Query functions for project CRUD"
      pattern: "db::schema"
---

<objective>
Scaffold the Rust project, define the complete SQLite schema, implement database initialization, and create the Axum server with project registration REST API and health endpoint.

Purpose: Establishes the foundation that all subsequent plans build on -- the compiled binary, database layer, shared state, and project management API. Every later plan imports types and uses infrastructure defined here.

Output: A running Axum daemon that serves static files, manages project registrations via REST, reports health, and has the full database schema ready for parser data.
</objective>

<execution_context>
@/home/sophia/.claude/get-shit-done/workflows/execute-plan.md
@/home/sophia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-and-state-pipeline/01-CONTEXT.md
@.planning/phases/01-backend-foundation-and-state-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust project scaffold, Cargo.toml, SQLite schema, and database layer</name>
  <files>
    Cargo.toml,
    build.rs,
    migrations/001_initial_schema.sql,
    src/main.rs,
    src/config.rs,
    src/state.rs,
    src/db/mod.rs,
    src/db/schema.rs,
    src/db/models.rs
  </files>
  <behavior>
    - Test: Database pool connects to SQLite in-memory and WAL mode is enabled
    - Test: Migration runs without error and creates all expected tables (projects, phase_state, plan_state, execution_runs, commits, agent_sessions, verification_results, parse_errors, project_config)
    - Test: Insert a project row and read it back correctly
    - Test: Insert phase_state, plan_state, execution_runs, commits rows with foreign key constraints honored
    - Test: Unique constraints on (project_id, phase_number) in phase_state enforced
    - Test: ON DELETE CASCADE removes child rows when project deleted
  </behavior>
  <action>
    1. Run `cargo init --name gsdui` to create the Rust project.

    2. Write Cargo.toml with all Phase 1 dependencies exactly as specified in 01-RESEARCH.md Standard Stack section:
       - axum 0.8 with "ws" feature
       - tokio 1 with "full" feature
       - tower 0.5
       - tower-http 0.6 with "fs", "cors", "compression-gzip", "trace" features
       - sqlx 0.8 with "runtime-tokio", "sqlite", "migrate" features
       - notify 8
       - serde 1 with "derive" feature
       - serde_json 1
       - serde_yml 0.0.12
       - tracing 0.1, tracing-subscriber 0.3 with "env-filter" feature
       - uuid 1 with "v4", "serde" features
       - chrono 0.4 with "serde" feature
       - regex 1
       - tokio-util 0.7
       - Dev deps: tempfile, reqwest (with "json" feature for API testing)

    3. Write build.rs with `cargo:rerun-if-changed=migrations` trigger.

    4. Write migrations/001_initial_schema.sql with the FULL schema from 01-RESEARCH.md:
       - projects table (id TEXT PK, name, path UNIQUE, status, retention_days, created_at, last_seen_at)
       - phase_state table (project_id FK, phase_number, phase_name, goal, depends_on JSON, stage, status, requirements JSON, plan_count, completed_plan_count, UNIQUE(project_id, phase_number))
       - plan_state table (project_id FK, phase_number, plan_number, plan_name, wave, depends_on JSON, plan_type, status, requirements JSON, files_modified JSON, UNIQUE(project_id, phase_number, plan_number))
       - execution_runs table (project_id FK, phase_number, plan_number, run_number, superseded, started_at, completed_at, duration_minutes, status, key_files_created JSON, key_files_modified JSON, requirements_completed JSON)
       - commits table (execution_run_id FK, task_number, task_name, commit_hash, commit_type)
       - agent_sessions table (project_id FK, agent_id, agent_type, phase_number, plan_number, started_at, ended_at)
       - verification_results table (project_id FK, phase_number, status, score, verified_at, UNIQUE(project_id, phase_number))
       - parse_errors table (project_id FK, file_path, error_message, severity, occurred_at, resolved_at)
       - project_config table (project_id FK, config_json TEXT, updated_at, UNIQUE(project_id))
       - All indexes from research

    5. Write src/db/models.rs with Rust structs for every table row:
       - Derive `sqlx::FromRow`, `serde::Serialize`, `serde::Deserialize` on each
       - Use `Option<T>` for nullable columns
       - Use `String` for TEXT, `i64` for INTEGER, `f64` for REAL
       - Include enums: ProjectStatus (active/offline), PhaseStage (planned/discussed/researched/planned_ready/executing/executed/verified), PlanStatus (pending/working/done/failed), VerificationStatus (passed/gaps_found/human_needed), ParseErrorSeverity (warning/error)
       - Implement Display and FromStr for enums (needed for SQLite TEXT storage)

    6. Write src/db/mod.rs:
       - `pub async fn init_pool(database_url: &str) -> Result<SqlitePool, sqlx::Error>` -- creates pool, runs migrations, sets WAL mode, synchronous=NORMAL, foreign_keys=ON, busy_timeout=5000
       - Re-export models and schema modules

    7. Write src/db/schema.rs with query functions:
       - Project CRUD: create_project, get_project, get_all_projects, update_project, delete_project
       - Phase state: upsert_phase_state, get_phase_states_for_project
       - Plan state: upsert_plan_state, get_plan_states_for_phase
       - Execution runs: insert_execution_run, update_execution_run, get_runs_for_plan
       - Commits: insert_commit, get_commits_for_run
       - Agent sessions: insert_agent_session, get_sessions_for_project
       - Verification: upsert_verification, get_verification_for_phase
       - Parse errors: insert_parse_error, resolve_parse_error, get_active_errors_for_project
       - Project config: upsert_config, get_config_for_project
       All functions take `&SqlitePool` as first parameter.

    8. Write src/config.rs:
       - `DaemonConfig` struct with fields: listen_addr (default "0.0.0.0:3000"), database_url (default "sqlite:data/gsdui.db?mode=rwc"), static_dir (default "static")
       - Parse from environment variables with GSDUI_ prefix (GSDUI_LISTEN_ADDR, GSDUI_DATABASE_URL, GSDUI_STATIC_DIR)

    9. Write src/state.rs:
       - `AppState` struct containing: db (SqlitePool), config (DaemonConfig)
       - Later plans will add broadcast and watcher fields

    10. Write src/main.rs as a minimal compilable entry point:
        - Initialize tracing_subscriber with EnvFilter
        - Load DaemonConfig from env
        - Create data/ directory if not exists
        - Initialize DB pool via db::init_pool
        - Build AppState (wrapped in Arc)
        - Create Router with /api/v1/ nested routes and static file fallback
        - Bind to configured addr and serve
        - Graceful shutdown on SIGINT/SIGTERM via tokio::signal

    Use Rust edition 2024 in Cargo.toml (edition = "2024"). Do NOT include `serde_yaml` (deprecated). Use `serde_yml` only.
  </action>
  <verify>
    <automated>cd /home/sophia/source/gsdui && cargo test --lib -- db 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Project compiles with `cargo build`
    - Database tests pass: pool creation, migration, CRUD operations, foreign key enforcement, cascade deletes
    - All 9 tables created with correct schema
    - All model structs derive FromRow + Serialize + Deserialize
    - Config loads from environment variables with defaults
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project REST API and health endpoint with integration tests</name>
  <files>
    src/api/mod.rs,
    src/api/projects.rs,
    src/api/health.rs,
    static/index.html,
    tests/api_integration.rs
  </files>
  <behavior>
    - Test: POST /api/v1/projects with {name, path} returns 201 with project object including generated UUID
    - Test: GET /api/v1/projects returns array of registered projects
    - Test: GET /api/v1/projects/:id returns single project
    - Test: DELETE /api/v1/projects/:id returns 204
    - Test: POST /api/v1/projects with duplicate path returns 409
    - Test: GET /api/v1/health returns daemon uptime, db_size_bytes, ws_client_count (0 initially)
    - Test: GET / returns static index.html
  </behavior>
  <action>
    1. Write src/api/mod.rs:
       - `pub fn router() -> Router<Arc<AppState>>` that nests all API sub-routers under /api/v1
       - Include /api/v1/projects, /api/v1/health routes
       - Apply tower_http::cors::CorsLayer (permissive for development)

    2. Write src/api/projects.rs:
       - POST /api/v1/projects -- accepts JSON {name: String, path: String}, validates path exists on disk and contains .planning/ directory, generates UUID, calls db::schema::create_project, returns 201 + project JSON. If path already registered, return 409 Conflict.
       - GET /api/v1/projects -- calls db::schema::get_all_projects, returns JSON array
       - GET /api/v1/projects/:id -- calls db::schema::get_project, returns 200 or 404
       - DELETE /api/v1/projects/:id -- calls db::schema::delete_project, returns 204 or 404
       - PUT /api/v1/projects/:id -- update project name, retention_days, returns 200 or 404

    3. Write src/api/health.rs:
       - GET /api/v1/health -- returns JSON with: status ("ok"), uptime_secs (from AppState start_time), db_size_bytes (query sqlite page_count * page_size), ws_client_count (0 for now, placeholder), version (env!("CARGO_PKG_VERSION"))
       - Add `start_time: std::time::Instant` field to AppState

    4. Write static/index.html:
       - Minimal HTML placeholder: "GSD Pipeline UI -- Backend running. Frontend coming in Phase 2."
       - Include basic styling (dark background matching Aurora theme #040814)

    5. Update src/main.rs to wire the API router with static file fallback via tower_http::services::ServeDir.

    6. Write tests/api_integration.rs:
       - Helper function to spin up the server on a random port with an in-memory SQLite database
       - Use reqwest to test all endpoints
       - Test project CRUD lifecycle (create, list, get, update, delete)
       - Test duplicate path rejection (409)
       - Test health endpoint returns expected fields
       - Test static file serving (GET / returns HTML)
  </action>
  <verify>
    <automated>cd /home/sophia/source/gsdui && cargo test --test api_integration 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All REST endpoints respond with correct status codes and JSON payloads
    - Project registration validates path exists with .planning/ directory
    - Duplicate path returns 409 Conflict
    - Health endpoint reports uptime, db_size, version
    - Static index.html served at root URL
    - Integration tests pass
  </done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with no errors
- `cargo test` runs all unit and integration tests green
- `cargo run` starts the daemon, listens on port 3000
- `curl http://localhost:3000/` returns the placeholder HTML page
- `curl http://localhost:3000/api/v1/health` returns JSON with status "ok"
- `curl -X POST http://localhost:3000/api/v1/projects -H 'Content-Type: application/json' -d '{"name":"test","path":"/tmp/test-project"}'` returns 201 (after creating /tmp/test-project/.planning/)
</verification>

<success_criteria>
- Compilable Rust binary with all Phase 1 dependencies
- SQLite database with all 9 tables, WAL mode, proper indexes
- Full database query layer with typed functions for every table
- Project CRUD REST API at /api/v1/projects
- Health diagnostics at /api/v1/health
- Static file serving at root
- Graceful shutdown on SIGINT/SIGTERM
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation-and-state-pipeline/01-01-SUMMARY.md`
</output>
